{
  "name": "contrast-enhancement-raster-bit-ui",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "python -m pip install -r requirements.txt",
    "start": "streamlit run app.py"
  }
}
